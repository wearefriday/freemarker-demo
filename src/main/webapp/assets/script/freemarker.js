/**
 *
 *  Freemarker demo
 *
 *  @name freemarker
 *  @version 1.0
 *
 */

require(['jquery', 'compile'], function ($) {

    var win = window;
    var undef;
    var $win = $(win),
        doc = win.document,
        $doc = $(doc);

    $doc.ready(function () {

        // nothing here yet...
    });

});
